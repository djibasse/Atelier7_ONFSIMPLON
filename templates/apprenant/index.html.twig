{% extends 'base.html.twig' %}


{% block title %}Apprenant index{% endblock %}

{% block body %}
    <h1>Apprenant index</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th scope="col" _msthash="2567565" _msttexthash="49374">type</th>
      <th scope="col" _msthash="2568449" _msttexthash="329914">En-tête de colonne</th>
      <th scope="col" _msthash="2569333" _msttexthash="329914">En-tête de colonne</th>
      <th scope="col" _msthash="2570217" _msttexthash="329914">En-tête de colonne</th>
    </tr>
  </thead>
  <tbody>
    <tr class="table-active">
      <th scope="row" _msthash="2588638" _msttexthash="60931">actif</th>
      <td _msthash="2586090" _msttexthash="383591">Contenu de la colonne</td>
      <td _msthash="2586974" _msttexthash="383591">Contenu de la colonne</td>
      <td _msthash="2587858" _msttexthash="383591">Contenu de la colonne</td>
    </tr>

  </tbody>
</table>
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>Adresse</th>
                <th>Telephone</th>
                <th>Email</th>
                <th>DateNaissance</th>
                <th>LieuNaissance</th>
                <th>Sexe</th>
                <th>NiveauEtude</th>
                <th>Attentes</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for apprenant in apprenants %}
            <tr>
                <td>{{ apprenant.id }}</td>
                <td>{{ apprenant.nom }}</td>
                <td>{{ apprenant.prenom }}</td>
                <td>{{ apprenant.adresse }}</td>
                <td>{{ apprenant.telephone }}</td>
                <td>{{ apprenant.email }}</td>
                <td>{{ apprenant.dateNaissance ? apprenant.dateNaissance|date('Y-m-d') : '' }}</td>
                <td>{{ apprenant.lieuNaissance }}</td>
                <td>{{ apprenant.sexe }}</td>
                <td>{{ apprenant.niveauEtude }}</td>
                <td>{{ apprenant.attentes }}</td>
                <td>
                    <a href="{{ path('apprenant_show', {'id': apprenant.id}) }}">show</a>
                    <a href="{{ path('apprenant_edit', {'id': apprenant.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="12">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('apprenant_new') }}">Create new</a>
{% endblock %}
